name: verriflo_classroom
description: Flutter SDK for embedding Verriflo live video classrooms with enterprise-grade features.
version: 2.0.1
homepage: https://verriflo.com
repository: https://github.com/verriflo/flutter-sdk

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.4.0
  webview_flutter_android: ^4.10.11
  webview_flutter_wkwebview: ^3.10.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
